/**
 * Time difference scope.
 *
 * Defines whether the time difference is global across all logger instances or local to a single logger instance.
 */
export type LoggerTimeDiffScope = 'global' | 'local';
